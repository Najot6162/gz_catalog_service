syntax = "proto3";

package genproto;
option go_package="genproto/sms_service";

message Sms {
    string id = 1;
    string created_at = 2;
    string text = 3;
    repeated string recipients = 4;
}

message Empty {
    
}